This is a toy Scheme implementation in F#.

How capable is it? Not very:
* It has no reader, so you have to manually construct the AST before evaluating it.
* It has two (count 'em) primitive functions: '+ and '*.
